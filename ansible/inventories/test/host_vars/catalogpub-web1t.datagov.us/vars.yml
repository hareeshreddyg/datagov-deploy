# server readonly/writable type
readwrite_type: writable

# db credentials
CKAN_DB_SERVER: "{{ vault_ckan_db_server }}"
PYCSW_DB_SERVER: "{{ vault_pycsw_db_server }}"
catalog_db_user: "{{ vault_catalog_db_user }}"
catalog_db_pass: "{{ vault_catalog_db_pass }}"
catalog_db_name: "{{ vault_catalog_db_name }}"
catalog_pycsw_db_name: "{{ vault_catalog_pycsw_db_name }}"

# other
SOLR_SERVER: "{{ solr_master_server }}"

# extras
SITE_URL: "{{ vault_ckan_site_url }}"
API_KEY: "{{ vault_ckan_admin_api_key }}" 

