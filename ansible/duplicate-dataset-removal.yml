---
- name: Run script to Delete Duplicate Datasets
  hosts:  catalog-web
  remote_user: ubuntu
  become: yes
  become_method: sudo

  roles:
  - { role: software/dup-dataset-removal, tags: [provision, misc] }
